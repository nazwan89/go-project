---
phase: quick
plan: 260606-s7g
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/main.go
  - main.go
  - .air.toml
  - Dockerfile
  - README.md
  - docs/DEVELOPMENT.md
  - docs/GETTING-STARTED.md
  - docs/ARCHITECTURE.md
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "main.go exists at cmd/main.go and not at project root"
    - "go build ./cmd compiles successfully"
    - "All run/build commands in docs and tooling reference ./cmd"
    - "CLAUDE.md entry point location reflects new path"
  artifacts:
    - path: "cmd/main.go"
      provides: "Application entry point"
    - path: ".air.toml"
      provides: "Updated hot-reload build command"
      contains: "go build -o ./tmp/main ./cmd"
    - path: "Dockerfile"
      provides: "Updated container build command"
      contains: "go build -o project ./cmd"
  key_links:
    - from: ".air.toml"
      to: "cmd/main.go"
      via: "go build ./cmd"
    - from: "Dockerfile"
      to: "cmd/main.go"
      via: "go build ./cmd"
---

<objective>
Move main.go from the project root into cmd/main.go following standard Go project layout, then update all tooling and documentation references to the new path.

Purpose: Adopts the conventional Go layout where main packages live under cmd/, making the project structure immediately recognizable to any Go developer.
Output: cmd/main.go, updated .air.toml, Dockerfile, README.md, docs/, and CLAUDE.md with no remaining root-level main.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move main.go and update build tooling</name>
  <files>cmd/main.go, main.go, .air.toml, Dockerfile</files>
  <action>
1. Create the `cmd/` directory at the project root.

2. Copy the full contents of `main.go` into `cmd/main.go`. The package declaration stays `package main` — no change needed. All imports (`project/config`, `project/module/sample`, `project/utils`) remain identical because they are module-absolute paths.

3. Delete the root-level `main.go`.

4. In `.air.toml` line 11, change:
   `cmd = "go build -o ./tmp/main ."`
   to:
   `cmd = "go build -o ./tmp/main ./cmd"`

5. In `Dockerfile` line 14, change:
   `RUN CGO_ENABLED=0 GOOS=linux go build -o project .`
   to:
   `RUN CGO_ENABLED=0 GOOS=linux go build -o project ./cmd`

6. Verify the build compiles: run `go build ./cmd` from the project root. It must succeed with exit code 0 and produce no binary at root (the cmd path targets the cmd package, output goes to GOPATH or current dir by default — just confirm no errors).
  </action>
  <verify>
    <automated>cd "/Users/nazwanibrahim/Documents/MRSB/SWDEV/BASED PROJECT/go-project" && go build ./cmd && echo "BUILD OK" && ls cmd/main.go && ! ls main.go 2>/dev/null && echo "ROOT MAIN GONE"</automated>
  </verify>
  <done>cmd/main.go exists, root main.go removed, go build ./cmd exits 0, .air.toml and Dockerfile use ./cmd build target.</done>
</task>

<task type="auto">
  <name>Task 2: Update documentation references</name>
  <files>README.md, docs/DEVELOPMENT.md, docs/GETTING-STARTED.md, docs/ARCHITECTURE.md, CLAUDE.md</files>
  <action>
Update every documentation file that references the old entry point location. Apply each change precisely — do not alter surrounding prose.

**README.md** — change `go run main.go` → `go run ./cmd` (lines ~53, 59, 254).

**docs/DEVELOPMENT.md** — change `go run main.go` → `go run ./cmd` (lines ~45, 72).

**docs/GETTING-STARTED.md** — change `go run main.go` → `go run ./cmd` (lines ~55, 158, 164).

**docs/ARCHITECTURE.md** — update path references to main.go:
- Line ~12: any diagram or code showing `main.go` at root → update to `cmd/main.go`
- Line ~40: prose location reference → `cmd/main.go`
- Line ~68: any "Entry point: main.go" style reference → `cmd/main.go`

**CLAUDE.md** — update the Entry Points section (lines ~140-141):
- Location: `main.go` → `cmd/main.go`
- Trigger description: `go run main.go` → `go run ./cmd`

**Do NOT change:**
- `test/module/sample/controller_test.go` line 16 (comment "mirrors main.go" is conceptually accurate, leave it)
- Any inline code comments inside `cmd/main.go` itself
  </action>
  <verify>
    <automated>cd "/Users/nazwanibrahim/Documents/MRSB/SWDEV/BASED PROJECT/go-project" && grep -rn "go run main\.go" README.md docs/ CLAUDE.md 2>/dev/null | wc -l | xargs -I{} sh -c 'if [ {} -eq 0 ]; then echo "DOCS OK - no stale go run main.go"; else echo "FAIL - {} stale references remain"; exit 1; fi'</automated>
  </verify>
  <done>No file in README.md, docs/, or CLAUDE.md contains "go run main.go". All run commands reference ./cmd. ARCHITECTURE.md and CLAUDE.md show entry point at cmd/main.go.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem | File move and text edits only — no network, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-s7g-01 | Tampering | cmd/main.go | accept | Identical content moved from root — no logic change, no new attack surface |
</threat_model>

<verification>
After both tasks:

1. `go build ./cmd` exits 0 from project root
2. `ls main.go` returns "No such file or directory"
3. `ls cmd/main.go` succeeds
4. `grep -rn "go run main.go" README.md docs/ CLAUDE.md` returns zero matches
5. `grep -n "go build" .air.toml` shows `./cmd` target
6. `grep -n "go build" Dockerfile` shows `./cmd` target
</verification>

<success_criteria>
- cmd/main.go exists with identical content to the former root main.go
- Root main.go is deleted
- `go build ./cmd` compiles cleanly
- All `go run` commands in README.md, docs/, and CLAUDE.md use `go run ./cmd`
- ARCHITECTURE.md and CLAUDE.md entry point location shows `cmd/main.go`
- .air.toml and Dockerfile build from `./cmd`
</success_criteria>

<output>
After completion, create `.planning/quick/260606-s7g-move-main-go-into-cmd-directory-and-upda/260606-s7g-SUMMARY.md` summarising what changed.
</output>
